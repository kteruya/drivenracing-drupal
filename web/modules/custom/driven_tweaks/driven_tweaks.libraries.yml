# =============================================================================
# driven_tweaks — Library definitions
# Version: 3.0.0 — 2026-04-27
#
# Phase 2 restructure: all CSS libraries removed.
# CSS/JS now lives in the driven theme (web/themes/custom/driven/).
#
# This file is kept because Drupal requires it if the module
# had libraries previously. It can be removed entirely once
# any config referencing driven_tweaks libraries is cleared.
# =============================================================================

# Intentionally empty — all libraries moved to driven theme.
# See web/themes/custom/driven/driven.libraries.yml
